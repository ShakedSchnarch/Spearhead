app:
  name: "Iron-View"
  version: "1.0.0"

paths:
  input_dir: "./data/input"
  output_dir: "./reports"
  templates_dir: "./templates"
  assets_dir: "./assets"

thresholds:
  erosion_alert: 0.75 # Example threshold

google:
  enabled: true              # Manual trigger via /sync/google or UI button
  service_account_file: null # Use API key if set in .env; else manual uploads
  api_key: null
  file_ids:
    platoon_loadout: ""          # placeholder – to be set per battalion loadout sheet if available
    battalion_summary: ""        # no battalion responses sheet; battalion = aggregate of the 3 companies
    form_responses: "1Jc8mEjAVMfuMoLTpVXG_C0anO1njy88q3MJfU6sTl3Y"  # responses (currently כפיר; סופה/מחץ to be added; no פלס\"ם yet)
  cache_dir: "./data/sync_cache"
  max_retries: 3
  backoff_seconds: 1.0
