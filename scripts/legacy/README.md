# Legacy Scripts

This folder contains historical scripts kept only for reference/migration support.

- They are **not** part of the current responses-only runtime.
- They are **not** used by the release/deploy flow.
- Active scripts are in `/scripts` and `/scripts/cloud`.
