version: "3.9"

services:
  ironview:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    command: >
      uvicorn iron_view.api.main:app --host 0.0.0.0 --port 8000
